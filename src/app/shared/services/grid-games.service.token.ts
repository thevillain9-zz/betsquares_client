import { InjectionToken } from '@angular/core';

/**
 * Token for Connected Component Service that allows for cleaner dependency injection.
 */
export const GridGamesServiceToken = new InjectionToken('IGridGamesService');